# Sessions Management System - Environment Configuration

# Database
POSTGRES_PASSWORD=your-secure-password-here
DATABASE_URL=postgresql+psycopg://sessions:your-secure-password-here@postgres:5432/sessions

# Backend
SECRET_KEY=your-secret-key-at-least-32-characters-long
DEBUG=false

# Google OAuth (for admin authentication)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=https://your-domain.com/api/v1/admin-auth/callback

# API Configuration
API_URL=http://backend:8000

# Public URLs (IMPORTANT: set these for production with https://)
# PUBLIC_BASE_URL is the public origin of the backend API
PUBLIC_BASE_URL=https://your-api-domain.com
# ADMIN_BASE_URL is the public origin of the admin portal
ADMIN_BASE_URL=https://your-admin-domain.com
# FRONTEND_BASE_URL is the public origin of the caregiver portal
FRONTEND_BASE_URL=https://your-frontend-domain.com

# Frontend SSR Configuration
# Public API URL for client-side code (baked in at build time by Vite)
PUBLIC_BASE_URL=https://your-api-domain.com
# Internal API URL for server-side rendering in Docker (overrides PUBLIC_BASE_URL on server)
# Set automatically in docker-compose.yml; no need to set manually in .env
# INTERNAL_API_URL=http://backend:8000

# Email Configuration (optional - not currently implemented)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# FROM_EMAIL=noreply@your-domain.com

# OpenTelemetry (optional)
# OTEL_ENABLED=true
# OTEL_EXPORTER_OTLP_ENDPOINT=http://your-otel-collector:4317
# OTEL_SERVICE_NAME=sessions-backend

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=60
MAGIC_LINK_RATE_LIMIT_PER_MINUTE=3
MAGIC_LINK_RATE_LIMIT_PER_HOUR=10
